# ========== ОСНОВНОЙ ФРЕЙМВОРК ==========
# FastAPI — сам фреймворк для создания API
fastapi==0.104.1
# Uvicorn — ASGI-сервер, запускает FastAPI приложение
# [standard] — дополнительные зависимости (websockets, httptools для скорости)
uvicorn[standard]==0.24.0

# ========== КОНФИГУРАЦИЯ ==========
# Pydantic-settings — чтение настроек из .env файла
pydantic-settings==2.1.0
# Python-multipart — нужен для обработки форм (загрузка файлов)
python-multipart==0.0.6

# ========== БАЗА ДАННЫХ ==========
# SQLAlchemy — ORM для работы с базой данных
# [asyncio] — асинхронный режим (чтобы не блокировать сервер во время запросов к БД)
sqlalchemy[asyncio]==2.0.23
# asyncpg — асинхронный драйвер PostgreSQL (быстрее чем psycopg2)
asyncpg==0.29.0
# Alembic — миграции базы данных (версионирование схемы)
alembic==1.13.0

# ========== БЕЗОПАСНОСТЬ ==========
# python-jose — библиотека для JWT (JSON Web Tokens)
# [cryptography] — криптографические алгоритмы для подписи токенов
python-jose[cryptography]==3.3.0
# passlib — хэширование паролей
# [bcrypt] — алгоритм bcrypt (устойчив к перебору)
passlib[bcrypt]==1.7.4

# ========== EMAIL ==========
# aiosmtplib — асинхронная отправка email
aiosmtplib==3.0.1
# Jinja2 — шаблонизатор для email-писем
jinja2==3.1.2

# ========== ФОНОВЫЕ ЗАДАЧИ ==========
# Celery — система очередей задач (отправка email, краулер по расписанию)
celery==5.3.4
# Redis — клиент для Python (брокер для Celery)
redis==5.0.1

# ========== ПЛАНИРОВЩИК ==========
# APScheduler — запуск задач по расписанию (краулер каждый час)
apscheduler==3.10.4

# ========== КРАУЛЕР ==========
# crawl4ai — LLM-friendly веб-краулер (из репозитория unclecode/crawl4ai)
crawl4ai==0.3.8
# aiohttp — асинхронные HTTP-запросы (нужен для краулера)
aiohttp==3.9.1
# BeautifulSoup4 — парсинг HTML (резервный вариант если crawl4ai не справится)
beautifulsoup4==4.12.2
# lxml — быстрый парсер для BeautifulSoup
lxml

# ========== ОТЧЁТЫ ==========
# Pandas — генерация отчётов (сводные таблицы)
pandas==2.1.3
# openpyxl — экспорт в Excel формат
openpyxl==3.1.2

# ========== РАБОТА С ФАЙЛАМИ ==========
# aiofiles — асинхронная работа с файлами (загрузка/выгрузка)
aiofiles==23.2.1
